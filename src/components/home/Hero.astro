<section class="hero">
	<div class="wrap">
		<div class="cols">
			<div>
				<div class="eyebrow eb">TCP · Go standard library · one process</div>
				<h1>A lightweight TCP server for named locks</h1>
				<p class="lead">— a distributed mutex without the distributed system.</p>
				<div class="actions">
					<a class="btn btn-primary" href="/start/quickstart/">Quick start</a>
					<a class="btn btn-secondary" href="/concepts/how-it-works/">How it works</a>
					<a class="btn btn-secondary" href="https://github.com/monolock-dev/monolock">GitHub</a>
				</div>
			</div>

			<div>
				<div class="panel">
					<div class="tbar">
						<span class="eyebrow-sm">Terminal</span>
						<span class="port">:7070</span>
					</div>
					<div class="tbody">
						<span class="prompt">$</span> docker run -p 7070:7070 \<br
						/>&nbsp;&nbsp;&nbsp;ghcr.io/monolock-dev/monolock
					</div>
				</div>
				<div class="stats">
					<div class="stat">
						<div class="num">1</div>
						<div class="cap">static binary</div>
					</div>
					<div class="stat">
						<div class="num">0</div>
						<div class="cap">dependencies, no database</div>
					</div>
					<div class="stat">
						<div class="num">0</div>
						<div class="cap">config files to write</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.wrap {
		padding-block: 108px 96px;
	}

	.cols {
		display: grid;
		grid-template-columns: minmax(0, 1.08fr) minmax(0, 0.92fr);
		gap: 72px;
		align-items: end;
	}

	.eb {
		margin-bottom: 26px;
	}

	h1 {
		margin: 0;
		font-size: clamp(42px, 5.4vw, 68px);
		line-height: 1.02;
		letter-spacing: -0.035em;
		font-weight: 600;
		max-width: 15ch;
	}

	.lead {
		margin: 26px 0 0;
		font-size: 21px;
		line-height: 1.5;
		color: var(--text-muted);
		max-width: 34ch;
	}

	.actions {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
		margin-top: 40px;
	}

	.tbar {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 10px 14px;
		border-bottom: 1px solid var(--border);
	}

	.port {
		font-family: var(--mono);
		font-size: 11px;
		color: #4f4a45;
	}

	.tbody {
		padding: 20px 18px;
		font-family: var(--mono);
		font-size: 13.5px;
		line-height: 1.7;
		color: var(--text);
		overflow-x: auto;
	}

	.prompt {
		color: var(--accent);
	}

	.stats {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1px;
		margin-top: 20px;
		background: var(--gutter);
		border: 1px solid var(--gutter);
		border-radius: 8px;
		overflow: hidden;
	}

	.stat {
		background: var(--band);
		padding: 16px 14px;
	}

	.num {
		font-size: 20px;
		font-weight: 600;
		letter-spacing: -0.02em;
	}

	.cap {
		font-size: 12.5px;
		color: var(--text-small);
		margin-top: 4px;
		line-height: 1.35;
	}

	@media (max-width: 920px) {
		.wrap {
			padding-block: 72px 64px;
		}

		.cols {
			grid-template-columns: minmax(0, 1fr);
			gap: 48px;
		}
	}
</style>
